package algo;

public class PacMan {

    /**
     *
     * @param grid, eg: {
     *     {'o', '.', '.'},
     *     {' ', 'x', ' '},
     *     {'.', '.', 'o'}
     * }
     * @param level, eg: 2
     * @return score, eg: 224
     */
    public static int play(char[][] grid, int level) {
        int i, j, total,
        grid = new {{'o', '.', '.'},{' ', 'x', ' '},{'.', '.', 'o'}};
        for(int i = 0; i < tab.length; i++) {
	    for(int j = 0; j< tab[i].length; j++) {
                if (valeur case = '.'){
                    Total = total+1
                }
                if (valeur case = 'O'{
                    Total = total+10
                }
                if (valeur case = 'x'){
                    Total = total+100
                }				
           }
       }

        return Total;
    }
}
